#include <iostream> 
using namespace std; 
void Difference (){ 
    long long  int A,B,C,D;
    cin>>A>>B>>C>>D;
    cout<<"Difference = "<<(A * B) - (C * D);
}
int main (){
 Difference();
    return 0;
}
